
// This layout handles the language parameter but the main structure is in src/app/layout.tsx
export default function LangLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
